export default function getListStudents() {
  const objArrray = [];
  objArrray.push({ id: 1, firstName: 'Guillaume', location: 'San Francisco' });
  objArrray.push({ id: 2, firstName: 'James', location: 'Columbia' });
  objArrray.push({ id: 5, firstName: 'Serena', location: 'San Francisco' });

  return objArrray;
}
